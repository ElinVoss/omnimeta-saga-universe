name: Canon Integrity
on: [push, pull_request]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify foundation immutability (placeholder)
        run: echo "Add sha256sum -c hashes file when available"
